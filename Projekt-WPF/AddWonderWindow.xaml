<Window x:Class="Projekt_WPF.AddWonderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Height="600" Width="450"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Nazwa:" FontWeight="Bold"/>
                <TextBox Text="{Binding NewWonder.Name}" Margin="0,5,0,10" Padding="5"/>

                <TextBlock Text="Lokalizacja:" FontWeight="Bold"/>
                <TextBox Text="{Binding NewWonder.Location}" Margin="0,5,0,10" Padding="5"/>

                <TextBlock Text="Data budowy:" FontWeight="Bold"/>
                <TextBox Text="{Binding NewWonder.DateBuilt}" Margin="0,5,0,10" Padding="5"/>

                <TextBlock Text="Zdjęcie:" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                    <Button Content="Wybierz plik..." Command="{Binding SelectImageCommand}" Padding="10,5"/>
                    <TextBlock Text="{Binding NewWonder.ImagePath}" VerticalAlignment="Center" Margin="10,0,0,0" FontStyle="Italic" TextTrimming="CharacterEllipsis" MaxWidth="250"/>
                </StackPanel>

                <Border BorderBrush="Gray" BorderThickness="1" Height="150" Margin="0,0,0,10" Background="#F0F0F0">
                    <Image Source="{Binding NewWonder.ImagePath}" Stretch="Uniform"/>
                </Border>

                <TextBlock Text="Opis:" FontWeight="Bold"/>
                <TextBox Text="{Binding NewWonder.Description}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,5,0,10" Padding="5"/>

                <Button Content="ZAPISZ" Command="{Binding SaveCommand}" Height="40" Margin="0,10,0,0" Background="#DDDDDD"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>