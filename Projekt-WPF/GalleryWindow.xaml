<Window x:Class="Projekt_WPF.GalleryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projekt_WPF"
        mc:Ignorable="d"
        Title="Galeria Cudów Świata" Height="650" Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="⬅ Menu" Command="{Binding NavigateBackCommand}" 
                HorizontalAlignment="Left" Padding="10,5" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="0" Text="Katalog Architektury" FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#444"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <ListBox Grid.Column="0" 
                     ItemsSource="{Binding Wonders}" 
                     SelectedItem="{Binding SelectedWonder}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     BorderThickness="0" Background="Transparent">

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Width="160" Height="180" Margin="5" Background="White" 
                                BorderBrush="LightGray" BorderThickness="1" CornerRadius="5">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.2"/>
                            </Border.Effect>
                            <StackPanel>
                                <Image Source="{Binding ImagePath}" Height="120" Stretch="UniformToFill" Margin="2"/>
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" 
                                           HorizontalAlignment="Center" Margin="5" TextWrapping="Wrap" TextAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Grid.Column="1" Margin="15,0,0,0" Padding="15" Background="#FAFAFA" 
                    BorderBrush="LightGray" BorderThickness="1" CornerRadius="5">

                <Grid DataContext="{Binding SelectedWonder}">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" TextWrapping="Wrap"/>

                            <Image Source="{Binding ImagePath}" Height="200" Stretch="Uniform" Margin="0,0,0,15"/>

                            <TextBlock Text="Lokalizacja:" FontWeight="Bold" Foreground="Gray"/>
                            <TextBlock Text="{Binding Location}" FontSize="16" Margin="0,0,0,10"/>

                            <TextBlock Text="Data budowy:" FontWeight="Bold" Foreground="Gray"/>
                            <TextBlock Text="{Binding DateBuilt, StringFormat=d}" FontSize="14" Margin="0,0,0,10"/>

                            <Separator Margin="0,10"/>

                            <TextBlock Text="Opis:" FontWeight="Bold" Foreground="Gray"/>
                            <TextBlock Text="{Binding Description}" TextWrapping="Wrap" FontSize="14" Margin="0,5,0,0" LineHeight="20"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <TextBlock Grid.Column="1" Text="👈 Wybierz obiekt z listy, aby zobaczyć szczegóły." 
                       HorizontalAlignment="Center" VerticalAlignment="Center" 
                       Foreground="Gray" TextWrapping="Wrap" TextAlignment="Center" Margin="20">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedWonder}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

        </Grid>
    </Grid>
</Window>
